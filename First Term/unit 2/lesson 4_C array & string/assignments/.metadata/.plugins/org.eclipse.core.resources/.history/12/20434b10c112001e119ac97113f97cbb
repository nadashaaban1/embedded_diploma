#include <stdio.h>
#include <string.h>

int main()
{
	char str[100],rev[100];
	printf("Enter a string: ");
	fflush(stdin); fflush(stdout);
	gets(str);

	for(int i=0; i<strlen(str); i++)
	{
		rev[i]=str[strlen(str)-i];
	}
	printf("Reverse string is : %c",rev);
}


